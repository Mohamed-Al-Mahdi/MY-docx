# markdown most use stuff

> it's a way to write in markdown fromat

## headers 


there is six size of headers 
### how to use it 
it neen to use this simple "#"
the more you is it the smaller the txt get 

example

  # # one header
  ## ## two headers
  ### ### three headers
  #### #### four headers
  ##### ##### five headers
  ###### ###### six headers

#### why to use it  

will its from it's name it for the page **head** or for the **subtitle** 

---

## Blockquote

it will make you use `>` his will make a dark box 

example 

> hello

-----

## cuting line 

to add a cutting line to cut the page into sections 

### how to use 

just use `---` in an empty line

---

## bullet point and numbering system 

to make bullet point you simpley use one ` * ` befor the line you need with one space  

example 

* point 1
* point 2
* point 3

you can simply use numbers like:

1. first item

2. second

3. third

---

## text menplation 

### 1. ltalic
to use it you need to add one `*` befor and after the text with no spases 

* example  *text*

* **why to use it:**  it look catchy something like "*hay look here*"

### 2. bold 
to use it you need  to add two `**` befor and after the text with no spases 

* example  **text**
* **why to use it:**  it make the text look importint it's like "**hay im focs here**" 

### 3. or the two combind 

to use it you need  to add three `***` befor and after the text with no spases

* example  ***text***

* **Why to use it:** To make warnings shout out from the page. 

---

## code highlight 

it's realy semple you just need no put `` ` `` befor and after the code 

if  you have more then one line you need to use three ` ``` ` befor and after hte code 

example for one 
`code one`
`code two`
`code three`

example for three

```
def hello_world(text):
  print(text)

hello_world("print")
```
you can add colers if you put the name of the programing languag ` ```python ` right after the first three backticks

```python 
def hello_world(text):
  print(text)

hello_world("print")
```

#### why to use it  

it will makr the code and make it ez to find
